import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'om.alzaabi.rasedadmin',
  appName: 'راصد الإدارة',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;